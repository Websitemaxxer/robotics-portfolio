# Build Plan — Arduino Coding Challenges

Instead of weeks, this project grows one **challenge** at a time. Each is set with only a
goal + behaviour + pins, written from scratch, then reviewed.

| # | Challenge | Concepts | Done? | Notes |
|---|-----------|----------|:-----:|-------|
| 1 | Reaction Timer | `millis()` timing, `random()`, digital I/O, `Serial` | ☑ | Solved myself; first attempt didn't compile **and** had a `millis()` logic bug |
| 2 | (to be set) | — | ☐ | |
| 3 | (to be set) | — | ☐ | |

> The point isn't to rush through many — it's that each one is genuinely written by me,
> with the failures documented in the [build diary](BUILD_DIARY.md). The gap between "I
> thought this would work" and what actually compiled/ran is the evidence that matters.
