# 05: Hardware control

Talking to an Arduino from Python: pyserial basics, parsing serial data into numbers, sending commands that drive physical outputs, timing a control loop without blocking, and a simple state machine.

Not yet populated.
